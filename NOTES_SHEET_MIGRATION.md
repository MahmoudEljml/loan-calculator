# تحديث عرض الملاحظات - من الصفحة المنفصلة إلى Sheet

## الملفات التي تم تعديلها:

### 1. **InstallmentNotesSheet.tsx** (ملف جديد)
- Component جديد مسؤول عن عرض الملاحظات في Sheet
- يتضمن:
  - عرض قائمة الملاحظات
  - إضافة ملاحظة جديدة
  - حذف ملاحظة
  - معالجة الأخطاء والتحميل
- يستخدم `Sheet` من shadcn/ui بدلاً من صفحة منفصلة

### 2. **EditInstallmentPage.tsx**
**التغييرات:**
- ✅ إزالة logic صفحة الملاحظات (`isNotes` view)
- ✅ استبدال حالة `showNoteForm` بـ `showNotesSheet`
- ✅ تحديث `handleAddNote` و `handleDeleteNote` للعمل مع Sheet
- ✅ تغيير زر الملاحظات ليفتح Sheet بدل الانتقال للصفحة
- ✅ إضافة `InstallmentNotesSheet` component

**السلوك الجديد:**
```
عند الضغط على زر "الملاحظات" في صفحة التعديل:
✓ يتم فتح Sheet من اليمين
✓ البقاء في نفس الصفحة
✓ لا توجد حاجة لتغيير URL
```

### 3. **InstallmentsPage.tsx**
**التغييرات:**
- ✅ استيراد `InstallmentNotesSheet`
- ✅ إضافة حالات جديدة:
  - `notesSheetOpen`: التحكم في فتح/إغلاق Sheet
  - `selectedNotesInstallmentId`: تحديد القسط المراد عرض ملاحظاته
- ✅ تحديث `onActionClick` لفتح Sheet عند `action === 'notes'`
- ✅ إضافة `InstallmentNotesSheet` component مع دوال `onAddNote` و `onDeleteNote`

**السلوك الجديد:**
```
عند الضغط على زر "الملاحظات" في الجدول:
✓ يتم فتح Sheet من اليمين
✓ البقاء في نفس الصفحة
✓ عرض ملاحظات القسط المحدد
✓ إمكانية إضافة وحذف الملاحظات مباشرة
```

### 4. **InstallmentsTable.tsx**
- ✅ لا تغييرات - تعمل كما هي
- ✓ تستمر في استدعاء `onActionClick(id, 'notes')`
- ✓ التعامل مع الإجراء يتم في `InstallmentsPage.tsx`

## المنافع:

1. **تجربة مستخدم أفضل**
   - لا توجد حاجة لتحميل صفحة جديدة
   - الملاحظات تظهر في Sheet سريع الاستجابة

2. **الحفاظ على السياق**
   - البقاء في نفس الصفحة أثناء إدارة الملاحظات
   - لا فقدان للبيانات المدخلة في النموذج

3. **واجهة مستخدم موحدة**
   - استخدام Sheet من shadcn/ui المتسق مع باقي التطبيق

4. **سهولة الصيانة**
   - Component منفصل مسؤول عن الملاحظات فقط
   - سهل التعديل والتحديث مستقبلاً

## الطرق التي يمكن الدخول منها:
1. ✅ من زر الملاحظات في صفحة التعديل (`EditInstallmentPage.tsx`)
2. ✅ من زر الملاحظات في الجدول (`InstallmentsTable.tsx`)

كلا الطريقتين تفتحان نفس Sheet مع نفس الوظائف.
